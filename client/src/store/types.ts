export enum LoadingStatus {
  LOADED = 'LOADED',
  LOADING = 'LOADING',
  ERROR = 'ERROR',
  NEVER = 'NEVER',
  SUCCESS = 'SUCCESS',
}

export enum UserStatus {
  UNAUTHORIZED = 'UNAUTHORIZED',
  REGISTER = 'REGISTER',
}
